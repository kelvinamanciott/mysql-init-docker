## Commands

```sh
# first terminal
docker compose up --build

# second terminal
docker compose exec mysql bash
show databases;
use my_database;
show tables;
desc my_table;
select * from my_table;
```
